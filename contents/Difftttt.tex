% !TEX root = ../main.tex

\chapter{Differential Four-Top Production in Partial Run 3}

Following the inclusive observation of four-top-quark production in Run~2, the increased centre-of-mass energy and integrated luminosity delivered in Run~3 provide the opportunity to move beyond inclusive measurements and explore the detailed kinematic structure of this rare process. 
% While inclusive cross sections establish consistency with the SM prediction at the level of the total production rate, they do not fully exploit the rich dynamics of multi-top final states.

Four-top production is characterised by high jet multiplicities, large hadronic activity, and complex decay topologies. Differential measurements of observables such as the four-top invariant mass, global event activity variables, and leading-object kinematics probe the underlying production dynamics in greater detail. 
In particular, the high-energy tails of multi-top observables are especially sensitive to deviations from the SM prediction.

From a theoretical perspective, studies within the Effective Field Theory (EFT) framework have shown that four-top production provides enhanced sensitivity to higher-dimensional operators, notably through distortions in differential distributions at large momentum transfer~\cite{FourTopEFTStudy}. 
In such scenarios, modifications of the interaction structure can lead to characteristic enhancements in the high-mass region of the four-top invariant mass spectrum, while inclusive cross-section measurements alone may exhibit limited discriminating power. 
Differential observables therefore offer complementary and potentially more sensitive probes of new interactions involving the top quark.

With the improved statistical precision of Run~3 data, selected differential observables become experimentally accessible. However, the complexity of the four-top final state requires advanced reconstruction techniques and a robust unfolding procedure to correct for detector effects. 
This motivates the development of a dedicated framework for differential four-top measurements in Run~3, which is the focus of this chapter.

\section{Framework}

Here need some introduction to the framework for differential four-top measurements, including the choice of observables, event selection, reconstruction techniques, and unfolding methodology.

\section{Reconstruction of the Four-Top System}

In the inclusive four-top analysis, final states containing same-sign or multi-lepton (SS/ML) signatures provide the dominant sensitivity. 
The requirement of two leptons with the same electric charge or more strongly suppresses the large $t\bar{t}$+jets background, leading to improved signal purity and a substantial contribution to the overall significance of the measurement. 
Within this category, the dileptonic same-sign (2LSS) channel offers a favourable balance between statistical yield and reconstruction complexity. 
Compared to multi-lepton final states, it features a larger branching fraction while involving only two neutrinos, thereby reducing the degree of kinematic under-constraint.

These properties make the 2LSS channel particularly well suited for developing and validating a reconstruction-driven differential measurement strategy. 
At the same time, the reconstruction and unfolding framework presented here is designed to be extendable to multi-lepton final states, where the inclusion of additional channels may further enhance the statistical precision in future studies.

Four-top production in the 2LSS channel remains characterised by a highly complex final state, featuring a large number of jets, multiple $b$-quarks, and two neutrinos from leptonic top decays. 
The combinatorial ambiguity in assigning reconstructed objects to their parent top quarks grows rapidly with jet multiplicity, and the presence of two neutrinos introduces additional kinematic under-constraints. 
A stable differential measurement of observables such as the four-top invariant mass therefore requires a reconstruction strategy capable of performing global object assignment while consistently incorporating neutrino kinematics.
The following sections describe the final-state assignment and neutrino reconstruction strategies adopted in this work.

\subsection{Hypergraph-based Final-State Assignment}

The assignment of reconstructed objects to the underlying decay products of the four top quarks is fundamentally a combinatorial problem. 
Multiple jets and $b$-tagged objects must be grouped into four overlapping decay chains, and incorrect assignments can significantly degrade the resolution of reconstructed observables.

Recent advances in machine learning have led to powerful reconstruction techniques based on attention mechanisms and graph neural networks, such as SPANet~\cite{Fenton2022Permutationless,Shmakov2022SPANet} and Topograph~\cite{Ehrke2023Topological}. 
These methods model collider events as sets or graphs of final-state objects, exploiting relational information to infer the most probable decay structure. 
While such approaches significantly outperform traditional $\chi^2$-based reconstruction~\cite{ATLAS2017TopMassAllHadronic}, their relational modelling is typically formulated in terms of pairwise interactions between objects. 
Although the top quark decays via the two-body process $t \to Wb$, the subsequent $W$ boson decay (either hadronic or leptonic) produces two additional particles, leading to a multi-body final state. 
Such decay topologies are reconstructed indirectly through combinations of two-body correlations.

In this work, the hypergraph-based reconstruction framework HyPER (Hypergraph for Particle Event Reconstruction)~\cite{HyPER} is employed to directly encode multi-body decay topologies.
The key conceptual difference between conventional graph representations and the hypergraph formalism is illustrated in Fig.~\ref{fig:hyper_representation}. 
In a standard graph, final-state objects are represented as nodes connected by pairwise edges. 
In contrast, a hypergraph allows a single hyperedge to connect multiple nodes simultaneously, naturally representing multi-body decay structures such as a top quark candidate formed from three objects.

\begin{figure}[htbp]
    \centering
    \begin{subfigure}{0.45\textwidth}
        \centering
        \includegraphics[width=\textwidth]{figures/graph_rep.pdf}
        \caption{Graph representation}
        \label{fig:hypergraph_representation}
    \end{subfigure}
    \hfill
    \begin{subfigure}{0.45\textwidth}
        \centering
        \includegraphics[width=\textwidth]{figures/hypergraph_rep.pdf}
        \caption{Hypergraph representation}
        \label{fig:hypergraph_representation}
    \end{subfigure}
    \caption{Schematic illustration of an event represented as (a) a graph and (b) a hypergraph. 
            In the graph representation, final-state objects are connected through pairwise edges. 
            In the hypergraph representation, candidate parent particles are represented by hyperedges connecting multiple objects simultaneously. 
            Dashed gray connections indicate alternative candidate combinations, while colored connections illustrate the true decay structure. 
            Taken from Ref.~\cite{HyPER}.}
    \label{fig:hyper_representation}
\end{figure}

The reconstruction proceeds in two stages, as schematically shown in Fig.~\ref{fig:hypergraph_architecture}. 
First, a message-passing phase is applied on the underlying graph representation, allowing information to propagate between pairwise-connected objects and enabling the network to learn both local and global event features. 
Second, higher-order candidate structures are constructed as hyperedges corresponding to possible parent particles. 
These hyperedges are classified by the network, and the highest-scoring, non-overlapping hyperedges are selected as reconstructed top-quark candidates.

\begin{figure}[htbp]
\centering
\includegraphics[width=0.4\textwidth]{figures/HyPER_layout.pdf}
\caption{Schematic architecture of the HyPER reconstruction framework. 
        Taken from Ref.~\cite{HyPER}.}
\label{fig:hypergraph_architecture}
\end{figure}

This two-step procedure allows higher-order decay structures to be inferred on top of a learned relational representation of the event. 
Such higher-order relational modelling is particularly well suited to four-top production, where multiple heavy resonances decay simultaneously in a high-multiplicity environment. 
The hypergraph formulation therefore provides a natural and flexible representation of complex decay topologies and serves as the basis for the reconstruction-driven differential measurement strategy developed in this work.

The hypergraph-based reconstruction strategy is adapted to the specific topology of four-top production in the 2LSS channel. 
In this final state, two top quarks decay hadronically, $t \to b q q'$, while the remaining two decay leptonically, $t \to b \ell \nu$. 
The reconstruction formalism is therefore constructed to reflect this mixed decay structure.

For hadronic decays, all final-state objects are directly observable. 
Each hadronic top candidate is constructed as a hyperedge connecting three jet nodes. 
Within each triplet, pairwise relational scores are evaluated to identify the most probable jet pair corresponding to the intermediate $W$ boson, while the remaining jet completes the three-body top candidate. 
This construction naturally encodes the multi-body decay topology while allowing the internal structure of the candidate to be determined by the learned relational features.

In contrast, leptonic top decays involve an undetected neutrino. 
Since the full three-body system cannot be constructed from visible objects alone, leptonic top candidates are initially represented by pairwise edges connecting a jet and a charged lepton. 
The missing neutrino kinematics are reconstructed in an additional step using dedicated algorithms, after which the complete leptonic top four-momentum is determined.

This mixed hyperedge–edge representation enables simultaneous treatment of fully visible hadronic decays and partially observed leptonic decays within a common graph–hypergraph structure. 
It provides a flexible and topology-aware reconstruction strategy tailored to the complex final state of four-top production.

\subsubsection{Performance of the Final-State Assignment}
- efficiency vs matching
- efficiency vs jet multiplicity

The performance of the hypergraph-based assignment is evaluated by matching reconstructed top candidates to their corresponding truth-level particles. 
A reconstructed top quark is considered correctly assigned if all associated visible decay products originate from the same truth-level top quark.

The matching efficiencies for hadronic and leptonic top candidates are evaluated separately. 
Hadronic tops are considered correctly reconstructed when the three selected jets correspond to the truth-level decay products. 
For leptonic tops, correctness is defined based on the assignment of the $b$-jet and charged lepton, prior to neutrino reconstruction.

To assess the robustness of the reconstruction in increasingly complex final states, the matching efficiency is studied as a function of the reconstructed jet multiplicity. 
As expected, the efficiency decreases moderately with increasing jet multiplicity due to the growth of combinatorial possibilities.

The achieved matching performance demonstrates that the hypergraph-based strategy effectively resolves the dominant combinatorial ambiguities, providing a reliable basis for the reconstruction of differential observables.
The observed performance stability across jet multiplicities indicates that the relational representation scales well with event complexity.

\subsection{Neutrino Reconstruction}

While hadronic top candidates are fully reconstructed from visible decay products, leptonic top decays involve undetected neutrinos whose kinematics must be inferred from missing transverse momentum and mass constraints. 
In the 2LSS final state, two neutrinos are present, leading to an under-constrained system when only the measured missing transverse momentum is used.
To reconstruct the full leptonic top four-momenta, the neutrino kinematics must be inferred from the measured missing transverse momentum and reconstructed event information. 
Two complementary approaches are considered in this analysis: the Neutrino Weighting method, which searches for solutions consistent with the $W$-boson and top-quark mass hypotheses, and a machine-learning-based generative approach ($\nu^2$-Flows), which models the conditional distribution of neutrino momenta given the observed event features.
Both methods aim to reconstruct the neutrino momenta in a way that is compatible with the measured event information, thereby allowing the full leptonic top four-momenta to be determined and enabling the construction of fully reconstructed four-top candidates.

\subsubsection{Neutrino Weighting Method}

In the dileptonic topology, each leptonic top decay chain can be written as
%
\[
t_{1,2} \rightarrow b_{1,2}\,\ell_{1,2}\,\nu_{1,2},
\]
%
and the neutrino momenta are constrained by the invariant mass hypotheses
%
\[
m_{W,i}^2 = (p_{\ell_i}+p_{\nu_i})^2,\qquad
m_{t,i}^2 = (p_{b_i}+p_{\ell_i}+p_{\nu_i})^2,\qquad i=1,2,
\]
%
together with the transverse momentum relation
%
\[
\vec{p}_T^{\,\nu_1}+\vec{p}_T^{\,\nu_2}=\vec{E}_T^{\mathrm{miss}}.
\]
%
Since the longitudinal neutrino momenta are not directly measured, the Neutrino Weighting (NW) method~\cite{ATLAS2017TopMassAllHadronic} resolves the remaining degrees of freedom by scanning over the neutrino pseudorapidities $\eta_{\nu_i}$. 
In this analysis, $\eta_{\nu_i}$ is scanned in the range $-5\leq\eta_{\nu_i}\leq5$ using a uniform grid of 100 points. 
For each assumed $\eta_{\nu_i}$, the kinematic constraints reduce to a quadratic system, yielding up to two physical solutions for the transverse neutrino momentum components. 
Candidate solutions for the two decay chains are then combined and evaluated at the event level.

To incorporate the finite widths of the intermediate resonances, the $W$-boson and top-quark masses are not fixed to single values but are sampled from Gaussian distributions centred on their nominal masses.
The Gaussian widths are taken from the Particle Data Group (PDG)~\cite{ParticleDataGroup:2024cfk} values for the physical decay widths of the $W$ boson and the top quark. 
An optional jet energy smearing step can also be applied to propagate jet resolution effects into the reconstruction.

Each candidate neutrino-pair solution is assigned a weight defined as a product of Gaussian terms,
%
\[
w=\prod_k \exp\!\left(-\frac{\Delta_k^2}{2\sigma_k^2}\right),
\]
%
where the index $k$ runs over the set of constraints included in the likelihood. 
In practice, the weight includes contributions from the agreement between the predicted and measured $x$ and $y$ components of $\vec{E}_T^{\mathrm{miss}}$, and an additional term comparing the azimuthal direction of the inferred neutrino system to the measured $\phi(\vec{E}_T^{\mathrm{miss}})$. 
Further terms evaluate the consistency of the reconstructed invariant masses of the two leptonic $W$ bosons and the two leptonic top quarks with the sampled mass hypotheses. 
The resolution parameters $\sigma_k$ are motivated by the expected detector resolutions for missing transverse momentum and invariant mass observables, and are chosen to provide a physically meaningful likelihood discrimination among candidate solutions.
The neutrino configuration with the maximum weight is selected as the reconstructed solution for the event, from which the full leptonic top four-momenta are determined.

\subsubsection{Generative neutrino reconstruction with \texorpdfstring{$\nu^2$}{nu2}-Flows}

As an alternative to the constraint-based Neutrino Weighting approach, a machine-learning-based generative method, $\nu^2$-Flows~\cite{Raine2023nu2flows}, has been proposed to reconstruct neutrino kinematics in under-constrained multi-neutrino final states.
The method employs conditional normalizing flows to model the distribution of possible neutrino momentum solutions conditioned on reconstructed event information, allowing neutrino hypotheses to be sampled on an event-by-event basis.

As illustrated schematically in Fig.~\ref{fig:nu2flows_arch}, the $\nu^2$-Flows model consists of two components: an event feature extraction network and a conditional normalizing flow.
The feature extraction network embeds the collections of reconstructed objects (jets, leptons, and $\vec{E}_T^{\mathrm{miss}}$) into a single event representation vector, which is then used as the conditioning input to the flow to learn the corresponding conditional density of neutrino solutions.
To accommodate variable jet and lepton multiplicities, the feature extraction stage is implemented with an attention-based architecture.
Object-specific embedding networks map each physics object to a latent token representation, which is subsequently processed by transformer encoder blocks.
A learnable class token is used together with cross-attention to obtain a single global event vector, ensuring permutation invariance with respect to the input object ordering.

\begin{figure}[htbp]
  \centering
  % Option A: if you redraw it yourself (recommended), include your own PDF/SVG export:
  \includegraphics[width=0.9\textwidth]{figures/nu2flow_layout.pdf}
  \caption{Schematic illustration of the $\boldsymbol{\nu^2}$-Flows architecture. 
  An event representation is constructed from reconstructed jets, leptons, and $\boldsymbol{\vec{E}_T^{\mathrm{miss}}}$ using an attention-based, permutation-invariant feature extractor with a learnable class token and cross-attention. 
  The resulting global vector conditions an invertible normalizing flow that models the conditional density of multi-neutrino momentum solutions. Taken from Ref.~\cite{Raine2023nu2flows}}
  \label{fig:nu2flows_arch}
\end{figure}

In the dilepton case, the conditional flow outputs a six-dimensional target corresponding to the three-momentum components of the neutrino and anti-neutrino, and can provide either the most probable solution or multiple sampled solutions per event.
% In this analysis, $\nu^2$-Flows is used as a complementary benchmark for neutrino reconstruction performance studies, while the nominal reconstruction employs the Neutrino Weighting method.

\subsubsection{Performance}

residual comparison on pxpypz

\subsection{Reconstruction of the Four-Top System}

The full reconstruction of the four-top system requires the determination of four top-quark four-momenta, comprising two hadronic and two leptonic decay chains. 
Following the hypergraph-based assignment and the neutrino reconstruction strategies introduced above, the complete event reconstruction is performed by combining these two components within a coherent workflow.

Two reconstruction workflows are considered, depending on the choice of neutrino reconstruction method, as illustrated in Fig.~\ref{fig:workflow_comparison}.

\paragraph{HyPER + Neutrino Weighting.}

In the nominal workflow adopted in this analysis, HyPER is first applied to determine the final-state assignment of visible objects. 
Hadronic top candidates are reconstructed as hyperedges connecting three jets, while leptonic candidates are identified through $b$-jet–lepton pairings. 
The Neutrino Weighting method is subsequently applied to each leptonic decay chain to infer the corresponding neutrino momenta, after which the full leptonic top four-momenta are constructed.

\paragraph{HyPER + \texorpdfstring{$\nu^2$}{nu2}-Flows.}

In the generative approach, neutrino kinematics are first inferred using the $\nu^2$-Flows model. 
The predicted neutrino four-momenta are then incorporated into the event representation used by HyPER, allowing both hadronic and leptonic top candidates to be reconstructed within a unified relational framework. 
In this workflow, the leptonic decay chains are reconstructed as complete multi-body structures once the neutrino hypotheses are provided.

In both workflows, the four reconstructed top-quark four-vectors are combined to form the total four-top system, from which the invariant mass $m_{4t}$ and other kinematic observables are derived.
% The Neutrino Weighting-based workflow is used as the nominal reconstruction strategy, while the $\nu^2$-Flows approach serves as a complementary performance benchmark.

\begin{figure}[t]
    \centering
    \begin{subfigure}{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{figures/Workflow_Hyper_plus_NW.pdf}
        \caption{HyPER + Neutrino Weighting workflow.}
        \label{fig:workflow_nw}
    \end{subfigure}
    \hfill
    \begin{subfigure}{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{figures/Workflow_Hyper_plus_Nu2.pdf}
        \caption{HyPER + $\nu^2$-Flows workflow.}
        \label{fig:workflow_nu2}
    \end{subfigure}
    \caption{Schematic illustration of the two reconstruction workflows considered for four-top reconstruction in the 2LSS channel. 
    The left panel illustrates the constraint-based workflow, where visible-object assignment is first performed by HyPER and the neutrino momenta are subsequently reconstructed using the Neutrino Weighting method.
    The right panel shows the generative workflow in which neutrino kinematics are inferred using $\boldsymbol{\nu^2}$-Flows prior to the hypergraph-based assignment.}
    \label{fig:workflow_comparison}
\end{figure}

\section{Profile Likelihood Unfolding Strategy}

\subsection{Definition of Differential Observables}

The primary observable considered in this analysis is the invariant mass of the four-top system, $m_{4t}$, defined at parton level. 
The parton-level top quarks are taken directly from the matrix-element record of the generator, prior to parton showering and decay. 
The four-top invariant mass is constructed from the sum of the four outgoing top-quark four-momenta,
\begin{equation}
m_{4t}^{\text{parton}} = \left( \sum_{i=1}^{4} p_{t,i}^{\text{ME}} \right)^2.
\end{equation}
This definition corresponds to the hard-scattering kinematics and defines the full phase space of the process.

At reconstruction level, a proxy observable $m_{4t}^{\text{reco}}$ is constructed from the fully reconstructed top-quark candidates obtained through the workflow described in Section~X. 
Event selection criteria are applied at reconstruction level, resulting in a restricted fiducial phase space. 
The unfolding procedure therefore extrapolates from the selected reconstruction-level phase space to the inclusive parton-level definition of $m_{4t}$.

In addition to the parton-level invariant mass, exploratory studies have been performed for selected particle-level observables, including the scalar sum of transverse momenta $H_T$ and the leading-lepton transverse momentum. 
These studies illustrate the flexibility of the unfolding framework in alternative kinematic configurations. 
However, as the emphasis of this thesis is on the development and validation of the differential reconstruction and unfolding methodology, detailed particle-level results are not presented here.

\subsection{Profile Likelihood Unfolding Formalism}

The differential cross section is extracted using the profile likelihood unfolding (PLU) approach. 
This method formulates the unfolding problem within the same statistical framework introduced in Eq.~\ref{eq:Likelihood}. 
While the inclusive measurement employs a single signal-strength parameter $\mu$ to scale the total signal yield, differential unfolding promotes this parameter to a set of independent signal-strength modifiers, one per truth bin.

The likelihood therefore becomes

\begin{eqnarray}
    L\left(\vec{n}|\vec{\mu}, \vec{\theta}\right) 
    &=& 
    \prod_{r \in \text{region}}
    \prod_{i\in \text{bin}} 
    \text{Pois}\left(n_{i,r} \mid 
    \sum_{j=1}^{N_\text{truth}}
    \mu_j \, S_{i,r}^{(j)}(\vec{\theta})
    + B_{i,r}(\vec{\theta})\right)
    \times
    \prod_{k\in \text{NP}}
    G\left(\theta_k\right).
\end{eqnarray}

Here, $S_{i,r}^{(j)}$ denotes the reconstruction-level template corresponding to truth bin $j$, obtained by folding the parton-level distribution through the detector response matrix. 
Each parameter $\mu_j$ scales the contribution of truth bin $j$, while $B_{i,r}$ represents the background prediction. 
The nuisance parameters $\vec{\theta}$ encode systematic uncertainties affecting both signal and background expectations, and are treated in the same way as in the inclusive fit.

In this formulation, unfolding reduces to a simultaneous maximum-likelihood determination of the normalization factors $\mu_j$, with systematic uncertainties profiled coherently across all bins and regions. 
The unfolded differential cross section in truth bin $j$ is then given by

\begin{equation}
    \sigma_j = \mu_j \, \sigma_j^{\text{MC}},
\end{equation}

where $\sigma_j^{\text{MC}}$ denotes the nominal Monte Carlo prediction for that bin. 
The fit therefore determines bin-by-bin scale factors that correct the predicted parton-level distribution to best describe the observed data.

This approach naturally incorporates detector response effects, background normalisation, and correlated systematic uncertainties within a unified statistical framework. 
Moreover, multiple regions or channels can be combined in a single fit, allowing the differential measurement to benefit from additional constraints and improved sensitivity.

In the absence of additional constraints, this formulation is equivalent to an unregularized matrix-inversion unfolding. 
Regularization can be introduced by adding additional penalty terms to the likelihood, as discussed in Section~3.4.

In order to determine the bin-wise signal-strength parameters $\mu_j$, 
the parton-level observable must be discretised and connected to the reconstruction-level measurement through the detector response. 
This requires a well-defined binning strategy and the construction of a response matrix encoding efficiency and bin migration effects.

\subsection{Binning Strategy and Response Matrix Construction}

The binning of the parton-level observable is optimised according to two primary considerations. 
First, each unfolded bin must contain sufficient statistical power to yield a meaningful uncertainty on the extracted cross section. 
Second, the detector response should remain well-conditioned, requiring a sufficiently large diagonal fraction in the migration matrix.

In the present partial Run~3 dataset, approximately twenty signal-region events remain after the full event selection. 
Given this limited statistical sample, a high-resolution binning would lead to unstable fits and large statistical uncertainties. 
To ensure a meaningful differential measurement while preserving sensitivity to shape variations, a minimal binning with three truth bins is adopted.

The bin boundaries are chosen to approximately balance the expected event yield across bins, resulting in the binning
\[
[0.0,\; 1379.243,\; 1813.773,\; 5000.0]~\mathrm{GeV}.
\]
With this choice, the expected number of events per bin is approximately uniform, corresponding to six to seven events per bin at reconstruction level. 
Although the resulting statistical uncertainties approach the nominal tolerance threshold, the diagonal elements of the migration matrix remain above 0.5, ensuring a sufficiently stable detector response. 
The corresponding response matrix is discussed in detail below.

This binning therefore represents a compromise between statistical precision and detector-level stability, and is adopted as the nominal configuration for the differential measurement.

The mapping from the truth-level distribution $T_j$ to the expected reconstructed yields $N_i$ is described through a response matrix $R_{ij}$,
\begin{equation}
N_i = \sum_{j} R_{ij}\,T_j .
\end{equation}
In practice, the response information can be provided either as the full matrix $R_{ij}$ or in a factorised form in terms of three ingredients: a migration matrix, a per-bin reconstruction-level efficiency, and an (optional) acceptance correction used to treat non-fiducial contributions.

The (unnormalised) migration matrix $M_{ij}$ encodes bin-to-bin migration from truth bin $j$ to reconstruction bin $i$. 
It is convenient to define a column-normalised migration matrix,
\begin{equation}
\widetilde{M}_{ij} = \frac{M_{ij}}{\sum_{i} M_{ij}},
\qquad \sum_i \widetilde{M}_{ij}=1,
\end{equation}
which represents the conditional probability of being reconstructed in bin $i$ given truth bin $j$ for events entering the reconstructed distribution.
The reconstruction-level selection efficiency is defined per truth bin as
\begin{equation}
\varepsilon_j \equiv P(\mathrm{selected}_{\mathrm{reco}} \mid j_{\mathrm{truth}}),
\end{equation}
i.e.\ the fraction of events in truth bin $j$ that pass the reconstruction-level selection and contribute to the fitted reconstructed distribution. 
With these definitions, the response matrix entering the likelihood model is constructed as
\begin{equation}
R_{ij} = \widetilde{M}_{ij}\,\varepsilon_j .
\end{equation}

If a fiducial truth-level phase space is defined, events outside the fiducial region may still pass the reconstruction-level selection and populate the reconstructed distribution. 
This effect can be handled via a per-bin acceptance factor defined on the reconstruction axis,
\begin{equation}
\alpha_i \equiv P(\mathrm{fiducial}_{\mathrm{truth}} \mid i_{\mathrm{reco}}),
\end{equation}
which quantifies the fraction of reconstructed events in bin $i$ originating from the fiducial region. 
This factor is applied as a correction to account for non-fiducial contamination. 
In the present analysis, the unfolding is performed to the full parton-level phase space and no truth-level fiducial restriction is imposed; consequently, this correction is not activated ($\alpha_i \equiv 1$), but the formalism is stated in this general form to allow future extensions.

The response information must be provided consistently for each systematic variation, since detector- and modelling-related uncertainties affect both the efficiency and the migration pattern and therefore the folded templates entering the fit.